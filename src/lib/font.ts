import { Metamorphous, Roboto } from "next/font/google";

export const metamorphous = Metamorphous({
  subsets: ["latin"],
  weight: "400",
});

export const roboto = Roboto({
  subsets: ["latin"],
  weight: "400",
});
